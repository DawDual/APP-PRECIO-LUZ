{
  "name": "1-app-precio-luz",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "concurrently \"vite\" \"nodemon backend/server.js\" \"json-server db.json --port 3001\"",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
  },
  "devDependencies": {
    "concurrently": "^8.0.1",
    "json-server": "^0.17.4",
    "nodemon": "^2.0.22",
    "vite": "^5.4.10"
  }
}
